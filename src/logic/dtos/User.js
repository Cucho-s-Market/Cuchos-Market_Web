// @ts-nocheck
class User {
    constructor(id, firstName, lastName, email, role, token) {
        this.id = id;
        this.firstName = firstName;
        this.lastName = lastName;
        this.email = email;
        this.role = role;
        this.token = token;
    }
}